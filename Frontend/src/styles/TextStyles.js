import { StyleSheet } from "react-native";

export const textStyle = StyleSheet.create({
    title: {
        fontFamily: "main-font",
        fontSize: 24,
        textAlign: 'center',
        marginBottom: 24,
    },
    button: {
        fontFamily: "main-font",
        textAlign: 'center',
    }
});
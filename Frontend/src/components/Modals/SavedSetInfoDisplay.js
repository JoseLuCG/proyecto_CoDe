import { View, Text } from "react-native";

export default function SavedSetInfoDisplay({setData}) {
    return(
        <View>
            <Text>This is the info display</Text>
        </View>
    );
}